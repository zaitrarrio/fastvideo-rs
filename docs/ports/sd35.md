# Stable Diffusion 3.5 — port specification

FastVideo family `sd35`: MMDiT (`SD3Transformer2DModel`) with triple text
encoders (CLIP-L, CLIP-G, T5-XXL) and Diffusers `AutoencoderKL` (16-ch).
Hub `stabilityai/stable-diffusion-3.5-medium`.

Sources (read 2026-09-22): FastVideo support matrix / `configs/pipelines/sd35.py`,
Diffusers `StableDiffusion3Pipeline`.

---

## Hub ids (registry)

| preset | Hub id | notes |
|---|---|---|
| `sd35_medium` | `stabilityai/stable-diffusion-3.5-medium` | T2I |

Default canvas **1024×1024**, FlowMatch Euler, guidance ≈ **4.5**, steps **40**.

---

## DiT (`SD3Transformer2DModel`) — Medium

| piece | value |
|---|---|
| `in/out_channels` | **16** / **16** |
| heads / head_dim / layers | **24** / **64** / **24** |
| `joint_attention_dim` | **4096** |
| `caption_projection_dim` | **1536** |
| `pooled_projection_dim` | **2048** |
| patch | **2** |
| sample_size | **128** (latent) |

VAE: AutoencoderKL SD3 (16ch, scale ≈ 1.5305). Text: CLIP-L + CLIP-G + T5-XXL.

---

## Port status (this tree)

| layer | status |
|---|---|
| Spec (this file) | landed |
| `fastvideo-models::sd35` | landed |
| Registry + CLI T2I path | landed |
| cudarc DiT (tiny zeros + load hook) | landed |
| Generate scaffold (noise → DiT → VAE → PNG) | landed |
| Triple text encode | external blocker (zeros tokens until encoders) |
| Full weight key parity | external blocker |
